# Continuous Value Extraction Operations

Wave58-63 把“15 个高价值上游项目的剩余价值榨取”变成持续运营面，而不是一次性审计。

## Operating Model

每个 value-ops 条目都必须回答：

- source project 是谁
- target plane 是什么
- 要吸收的增量价值是什么
- 明确不吸收什么
- 质量门槛是什么
- 下一步动作是什么

## Completion Rule

如果某条目只有“分析结论”没有产品化资产，则状态只能是 `candidate`，不能标记为 `absorbed`。
