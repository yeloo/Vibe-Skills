# Platform Replay Fixtures

These fixtures encode the **minimum contract-only** platform proof expectations for Batch C.

Important:

- This is not full cross-OS execution parity proof.
- It only verifies that the repo contains explicit, machine-readable statements
  about platform support boundaries (so documentation cannot drift into overclaim).

Validated by: `scripts/verify/vibe-universalization-no-regression-gate.ps1`

