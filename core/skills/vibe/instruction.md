# vibe canonical instruction

Use `vibe` when the task needs governed routing across planning, coding, debugging, review, or multi-agent execution.

Canonical rules:

- the canonical router remains authoritative
- provider-assisted intelligence may advise but must not replace route authority
- explicit user tool choice overrides routing
- XL execution may use multi-agent orchestration, but must preserve review and no-regression discipline

